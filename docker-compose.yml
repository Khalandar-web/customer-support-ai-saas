services:
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
   build: ./backend
   ports:
    - "8000:8000"
   environment:
    - AI_API_KEY=${AI_API_KEY}

  ai:
    build: ./ai-service
    ports:
      - "8001:8001"
